// pages reexports

import About from './About';
// Looks like ts in the IDE under the windows see no differencies 
// between folder './Index' and file './index.ts', so, ignore that ;))
// @ts-ignore:
import Index from './Index';

export { About, Index }
